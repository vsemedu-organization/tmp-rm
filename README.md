# Docker cron instance for cleaning folder files

[#] First of all, you have to build image;

```
$ docker-compose build
```

[#] Afterwards, you demand to emit container:

```
$ docker-compose up -d --force-recreate
```
